# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:<PASSWORD>@localhost:5432/weddingapp

# Backend port (Express)
PORT=5002

# Frontend port (Next.js)
FRONTEND_PORT=3002

# API proxy target (must match backend PORT)
API_TARGET=http://localhost:5002
