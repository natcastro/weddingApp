{
  "name": "wedding-app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "dotenv -e .env -- concurrently -k \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "dotenv -e .env -- cross-env PORT=3002 npm run dev --workspace=frontend",
    "dev:backend": "dotenv -e .env -- npm run dev --workspace=backend",
    "kill-ports": "lsof -ti:3002,5002 | xargs kill -9 2>/dev/null || true",
    "dev:fresh": "npm run kill-ports && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^7.4.2"
  }
}
